# 图表选择与编排

## 目录

- [先确定读者问题](#先确定读者问题)
- [架构与 UML 视图选择](#架构与-uml-视图选择)
- [Docs Engine 表达映射](#docs-engine-表达映射)
- [复杂度与拆图门槛](#复杂度与拆图门槛)
- [节点、分组和连线规则](#节点分组和连线规则)
- [架构文档的推荐组合](#架构文档的推荐组合)
- [发布前检查](#发布前检查)

## 先确定读者问题

生成图表前依次回答：

1. 读者是谁：产品、研发、运维、安全、外部集成方还是管理者？
2. 读者要回答什么问题：边界、依赖、调用顺序、状态、部署、数据归属还是交付时间？
3. 图的范围是什么：企业、系统、部署单元、模块、类、单次请求还是单个对象生命周期？
4. 哪一种关系最重要：包含、依赖、消息、控制流、数据流、状态迁移或时间？
5. 哪些细节不属于这张图？把它们移到下一层视图、表格或正文。

一张图只回答一个主要问题。标题应直接写明该问题，例如“Server 模块依赖方向”“设备首次绑定时序”“生产部署拓扑”，不要只写“架构图”或“流程图”。

## 架构与 UML 视图选择

| 读者问题 | 首选视图 | 关键元素 | 不要使用 |
| --- | --- | --- | --- |
| 一个概念、框架或分类如何分层 | Taxonomy / `mindmap` | 根主题、层级、同级分类 | 用多级 `flowchart` 重复相邻表格 |
| 系统服务谁、依赖哪些外部系统 | C4 System Context | 人、目标系统、外部系统、关系 | 内部类和接口清单 |
| 系统由哪些可部署/可运行单元组成 | C4 Container | 应用、服务、数据库、消息系统、协议 | 把源码目录当 Container |
| 一个部署单元内部有哪些职责模块 | C4 Component / UML Component | 模块、公开接口、单向依赖、边界 | 带十几个步骤编号的流程线 |
| 代码包如何分层、谁可以依赖谁 | UML Package / 模块依赖图 | Package、Group、允许方向、禁止方向 | 业务操作流程图 |
| 一次请求由谁按什么顺序完成 | UML Sequence / `sequenceDiagram` | 参与者、生命线、按时间排列的消息 | 横向平铺消息节点 |
| 一个对象有哪些状态及触发条件 | UML State Machine / `stateDiagram-v2` | 状态、事件、守卫、终态 | 把状态写成普通步骤 |
| 类型、接口、继承与组合是什么 | UML Class / `classDiagram` | 类、成员、继承、关联、多重性 | 展示运行时调用顺序 |
| 数据实体、字段和基数是什么 | ER / `erDiagram` | 实体、字段、主外键、基数 | 用箭头暗示不明确的数据关系 |
| 数据从哪里产生、转换和落地 | Data Flow / Pipeline | Source、Transform、Store、Sink | 同时混入部署和调用时序 |
| 业务步骤、条件和回退是什么 | Activity / `flowchart` | 动作、判断、分支、合并、终点 | 静态模块依赖 |
| 软件部署到哪些节点和网络区 | C4 Deployment / UML Deployment | 环境、节点、实例、网络边界 | 用源码模块代替运行实例 |
| 多角色之间如何交接工作 | Swimlane / 分组流程 | 责任泳道、步骤、交接 | 只有角色、没有顺序的关系图 |

UML 是语义工具箱，不是必须一次用齐的模板。C4 官方也明确不要求绘制全部层级；多数团队从 System Context 与 Container 开始，只在确有价值时增加 Component、Dynamic 或 Deployment 视图。

## Docs Engine 表达映射

| 目标视图 | 当前首选输入 | Board 语义 |
| --- | --- | --- |
| 分类 / 概念层级 | Mermaid `mindmap` | 根主题、父子层级和同级分类 |
| 业务流程 / Activity | Mermaid `flowchart` | 节点、条件边和方向 |
| 跨角色协议 / Dynamic | Mermaid `sequenceDiagram` | 参与者、生命线、纵向消息顺序 |
| 状态机 | Mermaid `stateDiagram-v2` | 状态节点和触发边 |
| 类和接口 | Mermaid `classDiagram` | 类型节点、成员和关系 |
| 数据模型 | Mermaid `erDiagram` | 实体节点、字段和基数 |
| 模块 / Package / Component | Mermaid `flowchart` + `subgraph`，或直接写 `BoardDocument` | Group/Container、成员节点、跨组依赖 |
| 精确设计的架构板 | 直接写 `BoardDocument` 与 authored layout | 作者位置、Group、锚点和路线 |

Mermaid 只是输入格式。不要依赖 Mermaid 运行时主题、SVG 输出或未验证的完整语法；输入会被导入成统一 BoardDocument，再由 Docs Engine 渲染和编辑。

静态架构关系使用 Group/Container 表达边界。不要为了得到一个外框把模块伪装成流程步骤，也不要用颜色代替分组名称。

### 分类框架与表格的分工

- 分类、能力树和概念框架使用 `mindmap`；只有存在先后、条件、汇合或反馈时才使用 `flowchart`。
- 不要用单根扇出的 `flowchart TB/LR`（一个根节点 `-->` 到多个分类，再各自展开）模拟纯层级树。这种写法会走通用图布局，根节点靠边、边被压成长横向捆绑线；同一内容写成 `mindmap` 走内置树布局，父子自动对齐、共享干线。判断标准：如果所有边都是“属于 / 包含”关系而没有任何流动方向，它就是层级，用 `mindmap`。
- 表格负责精确映射与逐项比较，图负责帮助读者先建立整体结构。相邻表格已经列出全部字段时，图只保留一层摘要节点，不再逐项复制表格。
- 一张概览图缩放后必须仍能直接读出一级结构。若为了容纳明细而把正文标签压到需要全屏才能辨认，提升抽象层级或拆图。

### Mermaid 作者契约与宿主一致性

- 在 Markdown / MDX 围栏中使用 `<br/>` 表达显式节点换行；不要写字面量 `\n`。Docs Engine 可以兼容旧内容，但新文档使用一种规范语法。
- 默认只提交语义源，让公开的 `Board importSource` 路径选择布局和路由。不要从 Showcase 复制“先导入、再删除坐标或路线”的回归夹具，也不要在依赖项目转换 `BoardDocument`。
- Showcase 中面向作者的示例必须使用已发布的公开 API。若测试必须直达内部回退路径，应明确标为引擎回归，并把核心断言放进自动化测试，而不是把私有处理伪装成推荐写法。
- 发布前同时检查组件 Showcase 与实际依赖宿主。以宿主正文列宽下的内嵌可读性为验收基线，再检查全屏编辑；不能用全屏可读替代正文可读。

### Agent 语义构图与自动路由

Agent 和 Docs Engine 按语义分工，不按“简单图 / 复杂图”粗暴分工：

- Agent 决定读者问题、视图范围、节点、Group、主方向和关系语义。
- Docs Engine 负责测量、层级、中心锚点、端口去冲突、正交避障、标签和边束。两条以上兼容边从同一侧离开同一节点时，自动使用“中心出口 → 共享干线 → 分支”；多条边进入同一侧时使用对称的汇流结构。若分支与多个兄弟节点共线，整束自动选择端口占用更少的外侧轨道，不能穿过中间节点，也不能贴着前序输入边形成难以辨认的平行线。
- authored layout 只表达无法从关系图推导的作者意图，例如固定区域、强调主链、指定部署边界位置或保留人工编排。它不是自动路由缺陷的兼容层。

Agent 构图按以下顺序执行：

1. 用 Mermaid 或 `BoardDocument` 固定稳定 ID、节点、Group、方向和关系，不先写坐标。
2. 把主链放在统一阅读方向；条件分支和合并都保留真实来源与目标。
3. 同一逻辑输出直接从同一节点声明多条边，同一逻辑输入直接连向同一目标；不要为了视觉效果添加假的 Hub、Bus 或 Junction 节点。
4. 真实存在的 Topic、队列、事件总线或网关必须保留为节点，不能与纯视觉边束混淆。
5. 首次渲染检查自动结果。若默认中心锚点、边束、避障或标签错误，修复 Docs Engine 并增加通用回归用例，不在依赖项目写手工 `points`。
6. 只有需要精确区域构图时才补 authored node positions；只有关系本身需要独立轨道时才补 edge anchors / `points`。
7. 调用 `validateBoardLayout()`，修复所有 error 级诊断，再检查正文宽度和全屏视图。
8. 持久化统一 `BoardDocument`；SVG/PNG 只能作为导出物。

反向成对边、反馈边、不同线型和不同阶段的边不会被自动合成同一边束。边束只是共享一段视觉路由，每条边仍保留自己的来源、目标、标签和箭头；不得用合束隐藏架构循环或丢失身份。作者提供的 `points` 约束中间弯点，不意味着独占节点中心端口；若入边与出边撞在同一侧，Docs Engine 会对称分配端点并同步校正首尾正交线段。

当两条走廊距离很近时，按以下顺序处理：

1. 同一来源的同阶段分流，或同一目标的同阶段汇流：合成共享干线。
2. 执行前输入与执行后分流、结果汇流与后续输出：保留为不同走廊，并优先使用节点的相对两侧。
3. 仍然拥挤：重新排布节点或 Group，让阶段顺序在空间上可读；不要用多个相差几像素的手工平行 `points`。

## 复杂度与拆图门槛

以下是默认写作门槛，不是运行时强制限制。触发任一项时，先拆图或提升抽象层级：

- 同一张图表达两种以上主要关系，例如同时表达模块依赖、运行时消息和部署位置。
- 节点超过 12 个、关系超过 15 条，且读者必须逐条追踪。
- 相邻表格已经完整表达同一映射，图仍逐项复制所有行或字段。
- 跨 Group 连线超过 3 条，或同一对 Group 之间出现多条方向不同的连线。
- 一条边标签需要两行以上才能读完，或边标签承载请求体、Token、字段清单等实现细节。
- 存在 8 个以上编号消息，仍试图使用 flowchart 或模块依赖图。
- 自动布局出现交叉线、回头线或共享线段，读者无法从起点连续追到终点。
- 为解释一张图需要连续写两段以上“某种线表示什么”。

拆图时保留稳定导航：先给高层边界图，再分别链接依赖图、关键时序图、状态图或部署图。不要简单把一张大图按屏幕区域切成多张，必须让每张图拥有独立问题。

## 节点、分组和连线规则

- 节点使用读者语言和稳定职责名称，不使用无意义内部 ID。
- Group 名称表示真实边界：部署单元、模块域、责任方、网络区或生命周期阶段。
- Group 内只放属于该边界的对象；跨组对象不能为了减少连线而重复出现。
- 静态依赖边使用“调用/读取/发布/实现”等关系语义；时序消息使用具体动作或接口。
- 线型最多表达一种额外维度，并在正文解释。例如实线表示业务依赖、虚线表示装配；不要再同时让颜色、粗细和箭头形状编码三套规则。
- 同源分流和同目标汇流使用中心锚点与共享视觉干线，但每条分支必须保留来源、目标、标签和箭头身份。
- 真实总线、Topic、队列或网关使用显式节点；不要把自动边束误写成一个不存在的架构组件。
- 时序图按时间向下；参与者按职责排列。请求与响应成对，长载荷移到正文或表格。
- 模块图保持依赖方向一致。出现循环时直接标记架构债务，不通过折线路由掩盖。

## 架构文档的推荐组合

一个完整架构解释页面通常从以下视图中选择 2–4 张，而不是画一张万能流程图：

1. System Context：产品、用户与外部系统边界。
2. Container 或部署单元图：进程、应用、数据库与协议。
3. Component / Package 图：目标模块内部的职责和允许依赖。
4. Sequence：一条最关键的运行时链路。
5. State：存在复杂生命周期时补充。
6. Deployment：生产环境、网络边界或容量是阅读重点时补充。

例如账号与设备身份文档应把“身份组件边界”与“首次绑定时序”分开；12 条编号消息属于 Sequence，不应压进横向组件图。模块化单体文档应把“模块 Owner/边界”与“一轮语音调用链”分开；组合根注入、业务依赖和遥测如果造成蜘蛛网，应拆成不同视图。

## 发布前检查

- [ ] 图表类型直接回答本节标题提出的问题。
- [ ] 每张图只有一个主要关系和一个抽象层级。
- [ ] 分类层级使用 `mindmap`，流程与分支才使用 `flowchart`；图没有逐项复制相邻表格。
- [ ] Mermaid 显式换行使用 `<br/>`，没有字面量 `\n`。
- [ ] 架构边界使用 Group/Container，而不是只靠节点颜色。
- [ ] 编号消息使用 Sequence，状态使用 State，字段基数使用 ER。
- [ ] 跨组边、循环和架构债务没有被自动布局隐藏。
- [ ] 边标签简短；接口载荷和字段细节已移到正文、表格或代码块。
- [ ] 正文给出读图结论，而不是逐像素描述图形。
- [ ] 同源分流和同目标汇流从中心锚点形成可追踪的边束，没有平行短桩或重复箭头。
- [ ] authored layout（如有）已通过 `validateBoardLayout()`，不存在节点重叠、穿线或无意共线。
- [ ] 构建后检查嵌入和全屏模式，确认标签、箭头、容器标题和生命线完整。
- [ ] Showcase 与消费项目均通过公开 API 渲染，正文列宽下无需先打开全屏即可读出一级结构。

## 标准依据

- [C4 model：Diagram levels and supporting diagrams](https://c4model.com/diagrams)
- [OMG UML 2.5.1 specification](https://www.omg.org/spec/UML/)
- [Mermaid flowchart and subgraph syntax](https://mermaid.js.org/syntax/flowchart)
- [Mermaid block diagram syntax](https://mermaid.js.org/syntax/block)
